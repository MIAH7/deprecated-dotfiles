alias toron="sudo torghostng -u -s -p -r -mac enp1s0 --dns"
alias toroff="sudo torghostng -x"
alias reflectorstart="sudo reflector --verbose --protocol https --latest 20 --sort rate --save /etc/pacman.d/mirrorlist"
